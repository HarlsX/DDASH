services:
  - type: web
    name: doodle-dash
    runtime: node
    plan: free
    buildCommand: echo "No build needed"
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: 22
